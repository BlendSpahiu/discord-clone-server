apiVersion: v1
kind: Service
metadata:
    labels:
        app: real-estate-actions-dev
    name: real-estate-actions-dev
    namespace: ornio-dev
spec:
    ports:
        - name: web
          protocol: TCP
          port: 5000
          targetPort: 5000
    selector:
        app: real-estate-actions-dev
